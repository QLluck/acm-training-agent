# Codex 开发任务：ACM Training Agent 可交互 Demo

> 建议仓库名：`acm-training-agent`  
> 当前目标：做一个可以给 ACM 集训队教练、队员、学校创业园老师现场演示的 Web Demo。  
> 重要原则：这不是 PPT，也不是纯宣传页，而是一个 **可以点击、可以切换角色、可以模拟完整训练闭环** 的产品原型。

---

## 1. 你的角色

你是一名资深产品经理、UI/UX 设计师和全栈工程师。

请直接在当前仓库中完成一个可运行的 Web Demo，不要只给方案，不要停留在文字说明。

如果当前仓库为空，请自行初始化项目。

在实现过程中优先保证：

1. 能运行；
2. 演示效果好；
3. 产品逻辑清晰；
4. Docker 部署简单；
5. 不依赖外部 API Key 也能完整演示；
6. 后续能够自然扩展成真实平台，而不是一次性静态网页。

---

# 2. 项目背景

我们准备做一个专门面向高校 ACM / 算法竞赛集训队的 AI 智能训练平台。

当前项目发起人本身就是高校 ACM 竞赛集训队负责人，因此拥有：

- 第一批真实用户；
- 真实训练场景；
- 教练指导资源；
- 队员历史训练数据；
- 真实比赛和训练反馈；
- 可以持续验证产品效果的环境。

这个产品不是为了替代 Codeforces、牛客、洛谷等刷题平台。

这些平台主要解决：

> “哪里有题可以刷？”

本产品真正要解决的是：

> “我今天到底应该刷什么？”  
> “为什么应该刷这些题？”  
> “我真正薄弱的能力是什么？”  
> “卡题后什么时候该给提示、给多少提示？”  
> “教练怎么快速知道每个队员和整个集训队的问题？”  
> “如何把教练长期积累的训练经验转化成可复用、可量化的训练策略？”

---

# 3. 传统 ACM 集训的核心问题

请在首页中明确展示这些痛点，并用 UI 视觉化表达，不要只是堆文字。

## 3.1 训练计划过于统一

传统训练中，教练经常给所有队员同一套题。

但是不同队员的：

- Rating；
- 知识点掌握程度；
- 代码实现能力；
- 比赛经验；
- 思维能力；
- 当前瓶颈；

完全不同。

结果是：

- 强队员觉得太简单；
- 中等队员大量重复已经掌握的内容；
- 新人做不动；
- 训练效率不高。

---

## 3.2 队员容易“盲目刷题”

很多队员拥有大量题库，却不知道：

- 当前最该提升什么；
- 下一题应该做什么；
- 应该刷专题还是打整场；
- 题目是不是太简单；
- 题目是不是跨度过大；
- 为什么做了几百题 Rating 仍然没有明显提升。

核心问题不是题少，而是：

> **缺乏训练决策。**

---

## 3.3 教练选题和管理成本高

教练需要人工完成：

- 观察每个队员；
- 选题；
- 分层训练；
- 安排周计划；
- 统计完成情况；
- 判断薄弱知识点；
- 赛后复盘；
- 调整训练节奏。

当队伍人数增加以后，这些工作很难做到真正个性化。

---

## 3.4 数据是碎片化的

一个队员可能在：

- Codeforces；
- 牛客；
- 洛谷；
- AtCoder；
- 校内 OJ；

分别训练。

数据分散之后，很难形成：

> 一份长期、统一、可解释的能力画像。

---

## 3.5 普通 AI 容易直接给答案

直接使用通用 AI 时，经常出现：

- 一卡题就直接给完整思路；
- 直接生成代码；
- 队员“看懂了”，但并没有真正学会；
- 没有根据队员水平控制提示程度；
- 不知道队员过去做过什么；
- 不知道当前训练计划。

这会降低真正的思考训练价值。

---

# 4. 产品核心定位

请在产品中使用下面的定位表达。

## 核心定位

**面向高校 ACM / 算法竞赛集训队的 AI 个性化训练与教练辅助系统。**

## 一句话价值

**不再盲目刷题，让每一道题都有训练目的。**

## 产品核心不是“大模型聊天”

真正核心应该体现为：

**训练数据 + 能力画像 + 教练训练策略 + AI Agent 工作流**

而大模型只是其中一个可替换的能力组件。

---

# 5. 产品核心闭环

请把整个 Demo 围绕下面的闭环设计：

```text
历史训练数据
    ↓
个人能力画像
    ↓
发现当前瓶颈
    ↓
生成个性化训练计划
    ↓
队员开始训练
    ↓
分级 AI 提示
    ↓
训练结果与错误分析
    ↓
更新能力画像
    ↓
生成下一轮训练计划
```

同时存在教练闭环：

```text
全队训练数据
    ↓
教练 Dashboard
    ↓
发现团队共同薄弱点
    ↓
调整训练策略
    ↓
生成周训练计划
    ↓
观察执行效果
```

---

# 6. Demo 必须展示的创新点

## 创新点 1：个人动态能力画像

每个队员不是只有一个 Rating。

系统应该至少展示以下维度：

- Dynamic Programming
- Graph
- Math
- Greedy
- Data Structure
- Implementation

用雷达图或其他直观方式展示。

Demo 中需要体现：

- 当前能力值；
- 最近 30 天变化；
- 最薄弱项；
- 最稳定项；
- 系统给出的简短诊断。

示例：

> 你的主要瓶颈不是代码实现，而是 DP 状态设计。  
> 最近两周图论明显进步，但计数 DP 仍存在连续卡题。

---

## 创新点 2：自适应训练计划

不要只是“推荐热门题”。

每一道推荐题都必须展示：

- 难度；
- 知识点；
- 推荐原因；
- 预计训练目标；
- 预计耗时；
- 当前状态。

例如：

```text
CF 1899F
难度：1600
知识点：DP / Greedy
推荐原因：
你近期 DP 基础题正确率较高，但综合状态设计题完成率不足。
本题用于训练“从局部决策抽象状态”的能力。
```

今日计划示例：

- 1 道热身题；
- 2 道核心训练题；
- 1 道综合题；
- 1 道挑战题。

---

## 创新点 3：分级 AI Hint，而不是直接给答案

训练页面需要模拟一个 AI Coach。

必须设计 4 个提示等级：

### Hint 1：问题引导

不给算法名称，只提出思考问题。

### Hint 2：方向提示

指出可能涉及的核心思想。

### Hint 3：结构提示

给出状态、关键观察或伪代码框架。

### Hint 4：复盘模式

只有明确选择“结束独立思考”以后，才展示比较完整的解析。

UI 上要强调：

> AI 的目标是帮助队员学会思考，而不是帮助队员 AC 一道题。

不要默认显示完整代码。

---

## 创新点 4：教练 Dashboard

这是整个 Demo 最重要的页面之一。

教练应该能够看到：

### 全队概览

例如：

- 队员人数：24
- 本周训练完成率：78%
- 本周提交数：463
- 平均首次 AC 率：61%
- 平均训练时长：9.7h
- 本周进步人数：17

### 全队知识点热力图

横轴：

- DP
- Graph
- Math
- Greedy
- DS
- Implementation

纵轴：

12 名模拟队员。

用热力图展示能力差异。

### 团队共同薄弱点

例如：

1. 计数 DP
2. 组合数学
3. 图上状态设计

### 需要关注的队员

例如：

- 连续 5 天未完成训练；
- 最近训练难度长期低于能力；
- 连续多题需要 Hint 3 以上；
- 比赛中前 30 分钟卡题率明显升高。

### AI 建议的下周训练计划

例如：

- 周一：组合数学基础
- 周三：计数 DP
- 周五：个人专项补强
- 周六：团队模拟赛
- 周日：赛后复盘

同时给出：

> 为什么这样安排。

---

## 创新点 5：把教练经验变成 Training Policy

在“创新点”页面展示这个概念。

示例：

```text
IF
DP mastery < 60
AND
Current Rating >= 1300
AND
Recent DP success rate < 50%

THEN
推荐：
2 × 1400 DP 基础
2 × 1500 DP 状态设计
1 × 1600 综合题
```

强调：

> AI 并不是凭感觉随机推荐，而是在教练训练策略约束下做个性化决策。

未来可以逐步把优秀教练的长期经验沉淀为：

- 训练规则；
- 难度调整规则；
- 新人培养路线；
- 区域赛训练路线；
- 专题训练策略；
- 比赛复盘策略。

---

## 创新点 6：未来的 AI 辅助出题

这个功能只做概念展示，不要在 Demo 中真正实现复杂出题系统。

明确表达：

不是：

> AI 自动生成一道题然后直接投入比赛。

而是：

> ACM 队员 / 教练提出创新想法，AI 辅助完成工程化出题流程。

未来工作流：

```text
人类提出题目创新点
    ↓
AI 辅助扩展题意与变形
    ↓
检索相似题
    ↓
生成初版标程
    ↓
生成暴力解
    ↓
随机对拍
    ↓
构造边界数据
    ↓
生成 Validator / Checker
    ↓
难度初步评估
    ↓
人类出题人最终审核
```

这个功能放在 Roadmap 中，不要喧宾夺主。

---

# 7. Demo 页面结构

至少实现以下页面。

---

## `/` 首页

目标：

让 CK 教练或学校创业园老师在 2 分钟内理解项目。

页面顺序建议：

### Hero

主标题：

> 不再盲目刷题  
> AI 为每一名竞赛队员设计真正需要的训练

副标题：

> 面向高校 ACM / 算法竞赛集训队的个性化训练与教练辅助平台

按钮：

- 查看队员端
- 查看教练端

同时显示一个醒目的：

> DEMO / Prototype

标识，避免让人误认为已经正式上线。

---

### 传统训练 vs Agent 训练

使用左右对比卡片。

传统训练：

- 全员同一题单；
- 队员自己找题；
- 卡题直接看题解；
- 教练手工统计；
- 训练效果难量化。

Agent 训练：

- 每人不同训练计划；
- 基于能力短板推荐；
- 分级 AI Hint；
- 自动生成能力画像；
- 教练实时掌握全队状态。

---

### 工作流程

用 6 步流程卡：

1. 导入训练数据
2. 建立能力画像
3. 识别训练瓶颈
4. 自动生成训练计划
5. AI 陪练与复盘
6. 持续更新能力模型

---

### 核心价值

分成：

#### 对队员

- 不知道刷什么 → 每天有明确训练计划；
- 不知道哪里弱 → 能力画像持续更新；
- 卡题就看题解 → AI 分级提示；
- 刷了很多没效果 → 每题都有训练目标。

#### 对教练

- 手工选题 → AI 辅助选题；
- 所有人同一计划 → 分层训练；
- 难以掌握全队 → Dashboard；
- 训练经验只在脑中 → Training Policy。

---

### 产品创新

展示 4~6 个创新点。

---

## `/student`

队员 Dashboard。

必须有：

### 顶部个人信息

模拟：

- 姓名：袁某
- Codeforces Rating：1548
- 目标：区域赛银牌
- 连续训练：12 天
- 本周完成率：83%

### 能力雷达图

6 个维度。

### AI 今日诊断

一句或两句简短分析。

### 今日训练计划

至少 5 道模拟题。

每道题有：

- 题目；
- 平台；
- Rating；
- 标签；
- 推荐原因；
- 状态；
- 开始训练按钮。

### 最近进步

折线图：

最近 8 周综合训练指数。

### AI 训练建议

例如：

> 暂时不要继续增加 DP 难度。  
> 建议先提高 1500~1600 难度题的独立完成率。

---

## `/student/session/1`

模拟实际刷题页面。

布局建议：

左侧：

- 题目信息；
- 难度；
- 知识点；
- 推荐原因；
- 计时器；
- 简化题面。

中间：

- 模拟代码编辑器；
- 可以使用 textarea 或简单编辑器；
- 不要求真正判题。

右侧：

### AI Coach

按钮：

- 我卡住了
- Hint 1
- Hint 2
- Hint 3
- 结束独立思考并复盘

逐步展开提示。

必须明显避免一上来展示答案。

底部：

- 标记 AC；
- 标记未完成；
- 写训练笔记。

操作后可以弹出模拟复盘卡片：

- 实际耗时；
- Hint 使用等级；
- 对能力画像的影响；
- 下一步推荐。

---

## `/coach`

教练 Dashboard。

这是演示重点。

必须实现：

### Overview Cards

展示：

- 队员人数
- 本周完成率
- 总提交数
- 平均首次 AC
- 平均训练时间
- 本周进步人数

### Team Skill Heatmap

12 名模拟队员 × 6 个能力维度。

### Team Weakness

柱状图或排序卡片。

### Student Watchlist

展示 3~4 个系统建议重点关注的队员。

每个人说明原因。

### Weekly Plan

展示 AI 给出的团队下周训练安排。

### Coach Decision Panel

提供几个模拟按钮：

- 接受建议
- 调整难度
- 给全队增加专题
- 为某队员单独加练

按钮不需要真正后端保存，但交互要有反馈。

---

## `/innovation`

用于给教练、创业园老师解释为什么这个项目不同。

展示：

1. 不是新的题库；
2. 不是 ChatGPT 套壳；
3. 不是普通刷题统计；
4. 核心是训练决策；
5. 教练经验可以沉淀为 Training Policy；
6. 队员数据会形成长期能力模型；
7. 未来可以接入多个 OJ；
8. 未来可以做 AI 辅助原创出题。

增加一个：

### 护城河 / 数据飞轮

```text
更多真实队员
    ↓
更多训练轨迹
    ↓
更准确的能力判断
    ↓
更好的训练策略
    ↓
更好的训练效果
    ↓
吸引更多队伍使用
```

同时强调：

> 真正的长期资产是训练数据、教练策略和训练效果验证，而不是某一个大模型。

---

# 8. 模拟数据要求

不要做空页面。

请内置一套有真实感的模拟数据。

至少 12 名队员。

每名队员包含：

```ts
{
  id,
  name,
  cfRating,
  level,
  weeklyHours,
  completionRate,
  firstAcRate,
  hintLevelAverage,
  streak,
  skills: {
    dp,
    graph,
    math,
    greedy,
    dataStructure,
    implementation
  },
  weaknesses,
  trend
}
```

请让不同队员能力分布明显不同。

例如：

- 新人；
- 中游队员；
- 高 Rating 队员；
- 实现强、思维弱；
- DP 强、图论弱；
- 长时间未训练；
- 做题很多但难度偏低。

这样教练 Dashboard 才有展示价值。

---

# 9. 技术栈要求

## 主语言

**TypeScript**

原因：

- 前端和服务端可以统一一种主要语言；
- Web 生态成熟；
- 适合快速做产品；
- 后续扩展全国 Web 平台没有问题；
- Next.js 可以直接做全栈；
- Docker 部署方便；
- 后续真正需要复杂 AI / 数据分析时，再把对应模块拆成 Python 服务。

不要为了“未来可能很大”而现在就做微服务。

---

## Web 框架

使用：

**Next.js 最新稳定版 + App Router + TypeScript**

要求：

- 使用 App Router；
- 使用 Server / Client Component 合理拆分；
- 不要使用过时 Pages Router；
- 不要依赖 Vercel 才能运行；
- 必须支持普通 Linux 服务器自托管。

---

## UI

建议：

- Tailwind CSS；
- Lucide Icons；
- Recharts。

如使用其他 UI 库，必须保持依赖简单。

视觉风格：

- 专业；
- 科技感；
- 竞赛训练感；
- 不要幼稚；
- 不要过度炫酷；
- 中文为主；
- 桌面端优先，同时具备基本响应式。

推荐基础视觉：

- 深蓝 / 石墨灰背景；
- 白色或浅色内容区；
- 少量蓝紫色强调；
- 卡片式 Dashboard；
- 清晰的数据可视化。

不要依赖远程 Google Fonts。

优先系统字体：

```css
font-family:
  Inter,
  ui-sans-serif,
  system-ui,
  -apple-system,
  BlinkMacSystemFont,
  "Segoe UI",
  "PingFang SC",
  "Microsoft YaHei",
  sans-serif;
```

---

# 10. Demo 阶段不要做的事情

当前版本不要实现：

- 真正的在线判题沙箱；
- 真正的 Codeforces 登录；
- OAuth；
- 支付系统；
- 大规模题库抓取；
- 真实大模型 API；
- RAG；
- 向量数据库；
- Kubernetes；
- 微服务；
- 完整权限系统；
- APP；
- AI 自动原创比赛题。

所有演示必须在：

> **没有任何 API Key 的情况下正常工作。**

所有 AI 内容先用预设模拟结果。

但代码结构要方便未来替换成真实 API。

---

# 11. 代码结构要求

请保持清晰。

建议：

```text
src/
  app/
  components/
  data/
  lib/
  types/
```

模拟数据集中放：

```text
src/data/
```

训练逻辑尽量通过函数封装，不要把大量业务逻辑全部写死在页面 JSX 中。

例如：

```text
src/lib/training/
```

可以预留：

- calculateSkillProfile
- generateTrainingPlan
- evaluateTrainingSession
- recommendTeamFocus

这些函数 Demo 阶段可以返回模拟结果，但接口设计要合理。

---

# 12. 为未来真实版本预留的架构

README 中增加“未来架构”章节，但不要现在实现。

未来可以逐步演进为：

```text
Browser
   ↓
Next.js Web Platform
   ↓
Application API
   ├── User / Team
   ├── Training Plan
   ├── Analytics
   └── Coach Policy
   ↓
PostgreSQL
```

AI 部分未来再拆：

```text
Next.js
   ↓
AI Service (Python / FastAPI)
   ├── LLM Gateway
   ├── Skill Diagnosis
   ├── Training Agent
   └── Problem Setter Assistant
```

缓存需求出现后再加入：

```text
Redis
```

全国化后再考虑：

- 对象存储；
- CDN；
- 消息队列；
- 多实例；
- 监控；
- 日志系统；
- Kubernetes。

当前 Demo **不要提前实现这些复杂基础设施**。

---

# 13. Docker 要求

这是硬性要求。

项目必须能够通过 Docker 部署。

创建：

```text
Dockerfile
.dockerignore
docker-compose.yml
```

Dockerfile 使用：

> **multi-stage build**

并尽量使用 Next.js standalone production output。

要求容器：

- 默认监听 `0.0.0.0:3000`；
- 无需运行时安装 npm 包；
- 无需 API Key；
- 容器启动后即可使用；
- 支持 `--restart unless-stopped`；
- 正常响应健康检查。

增加：

```text
GET /api/health
```

返回类似：

```json
{
  "status": "ok",
  "service": "acm-training-agent"
}
```

---

# 14. Docker 本地运行

README 必须给出完整命令。

至少支持：

```bash
docker build -t acm-training-agent:latest .
```

然后：

```bash
docker run -d \
  --name acm-training-agent \
  -p 3000:3000 \
  --restart unless-stopped \
  acm-training-agent:latest
```

访问：

```text
http://SERVER_IP:3000
```

同时支持：

```bash
docker compose up -d --build
```

---

# 15. 学校服务器“直接拉镜像部署”

这是重要需求。

请创建：

```text
.github/workflows/docker.yml
```

使用 GitHub Actions 构建 Docker Image 并推送到：

```text
GitHub Container Registry (GHCR)
```

要求：

- push 到 `main` 时构建；
- Git tag 时构建；
- 生成 `latest`；
- 生成 commit SHA 标签；
- 使用 GitHub 自带 `GITHUB_TOKEN`；
- README 说明如何把 Package 设置为 public。

镜像地址使用占位：

```text
ghcr.io/<github-user>/acm-training-agent:latest
```

学校服务器最终应该可以执行：

```bash
docker pull ghcr.io/<github-user>/acm-training-agent:latest
```

然后：

```bash
docker run -d \
  --name acm-training-agent \
  -p 3000:3000 \
  --restart unless-stopped \
  ghcr.io/<github-user>/acm-training-agent:latest
```

再增加一个：

```text
docker-compose.prod.yml
```

里面使用环境变量：

```text
ACM_AGENT_IMAGE
```

例如：

```bash
ACM_AGENT_IMAGE=ghcr.io/<github-user>/acm-training-agent:latest \
docker compose -f docker-compose.prod.yml up -d
```

---

# 16. 校园网环境特别要求

考虑服务器可能处于校园网环境：

1. **运行时不要依赖任何国外 CDN。**
2. 不要远程加载字体。
3. 不要依赖外部图片服务。
4. Demo 数据全部本地。
5. Docker Image 拉取完成后，即使服务器不能访问外部 API，也必须可以正常运行。
6. 所有 Icon 使用本地 npm 包。
7. 所有图片如非必要不要使用，视觉优先通过 CSS、图表、Icon 完成。
8. README 增加校园网部署排查：
   - 端口是否开放；
   - 防火墙；
   - Docker 是否监听；
   - `docker logs`;
   - `curl localhost:3000/api/health`;
   - 局域网访问 IP；
   - 如果有学校反向代理，如何把请求代理到 3000。

不要自动修改服务器防火墙配置，只提供说明。

---

# 17. README 要求

README 必须写清楚：

## 项目是什么

一句话定位。

## Demo 能展示什么

队员端、教练端、训练页、创新页。

## 为什么做

传统集训痛点。

## 技术栈

## 本地开发

```bash
npm install
npm run dev
```

## 生产构建

```bash
npm run build
npm run start
```

## Docker

完整命令。

## GHCR 镜像部署

完整命令。

## Demo 路线

给第一次展示的人建议：

```text
1. 首页
2. 队员端
3. 进入训练页面
4. 演示 Hint
5. 切到教练端
6. 查看全队能力热力图
7. 查看 Innovation
```

## 当前只是 Demo

明确写：

- 当前使用模拟数据；
- 没有真正调用大模型；
- 没有真正连接 OJ；
- 目标是先验证产品和训练逻辑。

## 下一阶段

- Codeforces 数据接入；
- 用户账户；
- 团队管理；
- 真实训练数据；
- LLM Hint；
- 教练 Training Policy；
- 实验验证。

---

# 18. 演示文案要求

请避免大量空洞营销语言。

页面应该让教练感觉：

> “这个东西真的是理解 ACM 集训的。”

多使用真实训练表达，例如：

- Rating；
- 补题；
- 专题训练；
- 模拟赛；
- 首次 AC；
- 卡题；
- Hint；
- DP；
- 图论；
- 数据结构；
- 训练完成率；
- 难度区间；
- 周训练计划。

不要把页面写成普通在线教育 SaaS。

---

# 19. 首页必须出现的核心对比

请设计一个高质量对比模块。

| 传统训练 | ACM Training Agent |
|---|---|
| 所有人同一题单 | 每个队员不同训练计划 |
| 队员自己找题 | 系统主动决定下一题 |
| 主要看 Rating | 多维动态能力画像 |
| 卡题直接看题解 | 分级 Hint |
| 教练人工统计 | 自动 Team Dashboard |
| 训练经验靠个人 | Training Policy 可沉淀 |
| 刷题数量导向 | 训练目标和效果导向 |

---

# 20. 首页必须出现的优势分析

至少展示：

## 训练效率

从：

> “大量刷题”

变成：

> “针对当前瓶颈训练”。

## 个性化

不同水平的人获得不同计划。

## 可解释

每一道推荐题必须告诉用户：

> 为什么推荐。

## 教练增效

减少人工选题、统计和观察成本。

## 长期数据资产

训练轨迹不断积累，形成个人长期能力模型。

## 教练经验资产化

教练经验不再只存在于个人脑中，而可以逐步形成结构化 Training Policy。

---

# 21. 产品真实性

不要伪造：

- 全国高校数量；
- 用户数量；
- 提升百分比；
- 商业收入；
- 已合作高校。

所有指标都明确标记为：

> Demo Data

或者：

> 模拟数据

未来真实试点后再替换。

---

# 22. 基本可访问性和质量要求

- 所有按钮有 hover / focus；
- 基本键盘导航可用；
- 图表有文字辅助解释；
- 不要使用明显过小字体；
- 页面在 1440×900 下效果优秀；
- 1366×768 也不能严重溢出；
- 手机端至少可基本浏览；
- 所有页面导航一致；
- 不允许大量死链接。

---

# 23. 验收标准

完成后请自行执行并修复问题：

```bash
npm run lint
npm run build
```

如果存在测试脚本，也运行测试。

然后验证 Docker：

```bash
docker build -t acm-training-agent:test .
```

如果当前环境能够运行 Docker，则启动容器并验证：

```bash
curl http://localhost:3000/api/health
```

必须确保：

- TypeScript 无明显错误；
- build 通过；
- 首页可访问；
- `/student` 可访问；
- `/student/session/1` 可访问；
- `/coach` 可访问；
- `/innovation` 可访问；
- Dockerfile 存在；
- docker-compose.yml 存在；
- GitHub Actions workflow 存在；
- README 完整。

---

# 24. 交付时不要只告诉我“完成了”

最终请给出：

1. 实际创建/修改的文件列表；
2. 技术栈；
3. 页面列表；
4. 本地启动方式；
5. Docker 启动方式；
6. GHCR 部署方式；
7. 当前哪些是模拟数据；
8. 下一步最值得接入的真实功能；
9. 如果有未完成项，明确列出来。

---

# 25. 开发优先级

严格按照：

## P0

- 项目能运行；
- 首页；
- Student Dashboard；
- Training Session；
- Coach Dashboard；
- Innovation；
- 模拟数据；
- Docker；
- README。

## P1

- 更好的图表；
- 更好的响应式；
- 更细致交互；
- Docker GHCR workflow；
- UI polish。

## P2

- 额外动画；
- 更多页面；
- 更多模拟分析。

不要为了 P2 耽误 P0。

---

# 26. 最重要的产品原则

请始终围绕这句话设计：

> **刷题平台解决“有题可刷”，ACM Training Agent 解决“今天该刷什么、为什么刷、怎么刷，以及教练如何判断训练是否有效”。**

这就是当前 Demo 最需要让人理解的创新价值。

现在请直接开始实现，不要只输出设计方案。
